apiVersion: v2
name: app
version: 0.1.0
appVersion: "1.0.0"
description: Generic Java application Helm chart with Prometheus metrics support
keywords:
  - java
  - spring
  - prometheus
  - micrometer
type: application

dependencies:
  - name: keda
    repository: https://kedacore.github.io/charts
    # Choose a stable 2.x version; adjust as needed. Using a broad constraint.
    version: ">=2.12.0 <3.0.0"
    condition: keda.install
